namespace CharacterAttributeSystemTest;

public class Modifiers
{
   /// <summary>
   /// Infinite modifiers doesn't expire (permanent curse for example) and should be removed directly (with curse removal spell for example)
   /// </summary>
   public const int Infinite = 0;
}

